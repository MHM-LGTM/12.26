# 动画系统完整规划方案（精简版）

## 1. 概述

本方案旨在为项目添加「我的动画」和「动画广场」两大功能模块，实现动画的创建、存储、分享功能。

### 核心特性
- **一键运行**：动画存储关键模拟信息，点击即可运行，无需重新上传图片、框选元素
- **私有空间**：「我的动画」仅用户自己可见可编辑
- **公共广场**：「动画广场」展示用户分享的作品
- **链接分享**：生成可分享的链接，指向网站运行动画
- **封面自动获取**：使用用户上传的物理题目图片作为封面，无需额外选择

---

## 2. 前端用户交互体验（重要）

### 2.1 整体页面布局

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  ┌─────────────┐                                    👤 138****8888  ⚙️ 设置  │
│  │  物理模拟   │                                                            │
│  └─────────────┘                                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌──────────────────────────────────────────┐   ┌─────────────────────────┐│
│  │                                          │   │      我的动画            ││
│  │                                          │   ├─────────────────────────┤│
│  │              物理模拟画布                  │   │ ┌─────┐ ┌─────┐ ┌─────┐ ││
│  │           （上传图片 → 分析 → 选择）       │   │ │封面 │ │封面 │ │封面 │ ││
│  │                                          │   │ │图片 │ │图片 │ │图片 │ ││
│  │                                          │   │ └─────┘ └─────┘ └─────┘ ││
│  │                                          │   │  动画1   动画2   动画3   ││
│  │                                          │   │                         ││
│  │                                          │   │ ┌─────┐ ┌─────┐ ╭ ─ ─ ╮ ││
│  │                                          │   │ │封面 │ │封面 │ │     │ ││
│  │                                          │   │ │图片 │ │图片 │ │ ... │ ││
│  │                       │                  │   │ └─────┘ └─────┘ ╰ ─ ─ ╯ ││
│  └───────────────────────┼──────────────────┘   └─────────────────────────┘│
│              ┌───────────┴───────────┐                                     │
│              │ 开始模拟 │  ⬇️ 下载    │  ← 下载按钮在首次模拟后显示          │
│              └───────────────────────┘                                     │
│                                                                             │
│  ════════════════════════════════════════════════════════════════════════  │
│  画布下方信息区（加载广场动画时显示）：                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  📝 弹性碰撞演示                                    ❤️ 128  👤 物理君  │   │
│  │  演示两个滑块在光滑水平面上的弹性碰撞过程...                          │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
├─────────────────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐                                                            │
│  │  动画广场   │                                                            │
│  └─────────────┘                                                            │
│                                                                             │
│  ┌───────────────┐ ┌═══════════════┐ ┌───────────────┐ ┌───────────────┐   │
│  │ ┌───────────┐ │ ║ ┌───────────┐ ║ │ ┌───────────┐ │ │ ┌───────────┐ │   │
│  │ │  封面图片  │ │ ║ │  封面图片  │ ║ │ │  封面图片  │ │ │ │  封面图片  │ │   │
│  │ └───────────┘ │ ║ └───────────┘ ║ │ └───────────┘ │ │ └───────────┘ │   │
│  │  弹性碰撞     │ ║  牛顿摆       ║ │  多米诺骨牌   │ │  自由落体     │   │
│  │  ❤️ 128       │ ║  ❤️ 89        ║ │  ❤️ 234       │ │  ❤️ 45        │   │
│  └───────────────┘ └═══════════════┘ └───────────────┘ └───────────────┘   │
│                     ↑ 选中状态：微微放大 + 高亮边框                          │
└─────────────────────────────────────────────────────────────────────────────┘
```

**区域说明：**
- **左侧大区域**：物理模拟画布（创作区）
- **右侧区域**：「我的动画」面板（私人动画库）- 位于画布正右侧
- **画布下方**：操作按钮区 + 动画信息区（加载广场动画时显示）
- **底部区域**：「动画广场」面板（公共动画库）

### 2.2 当前项目的完整工作流程（重要背景）

用户使用本项目进行物理模拟的完整流程如下：

```
步骤1：用户上传一张物理题目图片（包含模拟场景）
       → 这张图片同时作为动画的封面图
           ↓
步骤2：后端并行处理两件事：
       • 调用大模型（豆包多模态）分析图片，识别场景中的元素
         （例如：滑块A、滑块B、地面、斜面等）
       • 预热图片的 SAM embedding（为后续分割做准备）
           ↓
步骤3：后端返回识别结果给前端
       • elements_detailed: 元素列表（含名称、角色、物理参数等）
           ↓
步骤4：用户在画布上进行交互
       • 点击或框选图片中的物体
       • 后端调用 SAM 返回分割轮廓
       • 用户为每个轮廓选择对应的元素名称（从识别列表中选择）
       • 如果是特殊元素（如摆球），还需选择支点等约束关系
           ↓
步骤5：用户点击「开始模拟」
       • 前端将所有信息（图片路径、元素、轮廓、参数）发送到后端
       • 后端使用 OpenCV 处理：
         - 裁剪每个元素的精灵图
         - 消除动态物体，修复背景
       • 后端返回处理结果
           ↓
步骤6：前端缓存模拟数据，调用 Matter.js 物理引擎执行模拟
       • 模拟数据会被缓存到 simulationCache
       • 「下载」按钮此时显示出来
       • 用户再次点击「开始模拟」时，直接使用缓存，无需后端再次处理
```

### 2.3 动画系统的核心交互流程

#### 场景一：用户保存当前模拟为动画（下载）

```
前提：用户已完成步骤1-6，首次点击「开始模拟」后，「下载」按钮显示

步骤1：用户点击「下载」按钮
           ↓
步骤2：弹出保存弹窗（精简版）：
       ┌────────────────────────────────────┐
       │         💾 保存动画                 │
       ├────────────────────────────────────┤
       │                                    │
       │  ┌──────────────────────────────┐  │
       │  │    📷 封面预览                │  │  ← 自动使用上传的图片
       │  │   （用户上传的题目图片）       │  │
       │  └──────────────────────────────┘  │
       │                                    │
       │  动画名称 *                         │
       │  ┌──────────────────────────────┐  │
       │  │  弹性碰撞实验                 │  │
       │  └──────────────────────────────┘  │
       │                                    │
       │  描述（可选）                       │
       │  ┌──────────────────────────────┐  │
       │  │  两个滑块的碰撞演示...        │  │
       │  └──────────────────────────────┘  │
       │                                    │
       │        ┌────────┐  ┌────────┐     │
       │        │  取消  │  │  保存  │     │
       │        └────────┘  └────────┘     │
       └────────────────────────────────────┘
           ↓
步骤3：点击「保存」
       • 动画保存到「我的动画」
       • 弹出提示：「是否同时上传到动画广场？」
         ┌────────────────────────────────────┐
         │  ✅ 保存成功！                      │
         │                                    │
         │  是否将动画分享到动画广场？         │
         │  分享后其他用户也能看到并使用       │
         │                                    │
         │  是否公开显示你的用户名？           │
         │  ○ 显示用户名   ● 匿名             │
         │                                    │
         │    ┌────────┐  ┌────────────┐     │
         │    │ 暂不了 │  │ 分享到广场 │     │
         │    └────────┘  └────────────┘     │
         └────────────────────────────────────┘
           ↓
步骤4：动画出现在「我的动画」面板中
       如果选择分享，同时出现在「动画广场」
```

**【关键点】封面图自动获取：**
- 使用用户在步骤1上传的物理题目图片作为封面
- 无需用户额外选择或上传封面图

#### 场景二：用户从「我的动画」加载动画

```
步骤1：用户点击「我的动画」面板中的某个动画卡片
           ↓
步骤2：系统读取该动画的 scene_data
           ↓
步骤3：系统直接恢复模拟场景到画布：
       • 设置背景图（background_url）
       • 根据 objects 数据直接创建物理刚体
       • 根据 constraints 创建约束关系
       • 「下载」按钮显示（因为已有模拟数据）
           ↓
步骤4：用户可以直接点击「开始模拟」运行，或修改参数后运行

【关键点】
- 不需要重新上传图片
- 不需要重新调用大模型分析
- 不需要重新框选元素
- 不需要后端 OpenCV 处理
- 直接使用保存的数据恢复场景，一键运行
```

#### 场景三：用户从「动画广场」使用动画

```
步骤1：用户在「动画广场」浏览动画卡片
       • 每个卡片显示：封面图、名称、❤️点赞数
       • 可以直接点击卡片上的 ❤️ 进行点赞
           ↓
步骤2：用户点击某个动画卡片
       • 该卡片进入「选中状态」：微微放大 + 高亮边框
       • 动画直接加载到画布中
       • 画布下方显示动画信息区：
         ┌─────────────────────────────────────────────────┐
         │  📝 弹性碰撞演示                  ❤️ 128  👤 物理君 │
         │  演示两个滑块在光滑水平面上的弹性碰撞过程...      │
         └─────────────────────────────────────────────────┘
         - 动画名称
         - 动画描述
         - 点赞数（可点击点赞）
         - 作者用户名（如果作者选择公开；否则不显示）
           ↓
步骤3：用户可以进行以下操作：
       
       操作A - 点击「开始模拟」：
       • 直接运行动画
       
       操作B - 点击「下载」：
       • 将动画复制到「我的动画」（Fork）
       • 弹出提示：「已保存到我的动画」
       • 用户获得完整编辑权限
       
       操作C - 点击画布下方的 ❤️ 点赞：
       • 给动画点赞（需登录）
       
       操作D - 点击卡片上的 ❤️ 点赞：
       • 同样可以点赞
```

**【关键点】广场动画的交互简化：**
- 点击卡片 → 直接加载到画布（不弹窗）
- 卡片选中状态：微微放大 + 高亮边框
- 复用「下载」按钮实现 Fork 功能
- 两处可点赞：卡片上的 ❤️ 和画布下方的 ❤️

#### 场景四：用户分享动画链接

```
步骤1：用户在「我的动画」中选择一个动画
           ↓
步骤2：点击「分享」按钮（或在卡片菜单中选择）
           ↓
步骤3：弹窗显示分享链接：
       ┌────────────────────────────────────┐
       │  🔗 分享链接                        │
       ├────────────────────────────────────┤
       │                                    │
       │  https://physmath.cn/play/abc123   │
       │                          📋 复制   │
       │                                    │
       │  分享给朋友，他们可以直接运行动画   │
       │                                    │
       └────────────────────────────────────┘
           ↓
步骤4：朋友打开链接：
       • 跳转到网站的播放页面
       • 自动加载并运行该动画
       • 显示动画信息（名称、描述、作者）
       • 可以点击「下载」保存到自己的动画库（需登录）
```

### 2.4 各面板/区域的交互细节

#### 「下载」按钮

```
位置：画布下方，「开始模拟」按钮旁边
显示条件：用户首次点击「开始模拟」后显示
功能：
  • 如果是用户自己创建的模拟 → 保存到「我的动画」
  • 如果是从广场加载的动画 → Fork 到「我的动画」
样式：
  ┌───────────┐  ┌───────────┐
  │ 开始模拟 →│  │ ⬇️ 下载   │
  └───────────┘  └───────────┘
```

#### 「我的动画」面板

```
位置：页面右侧，物理模拟画布的正右侧
展示：网格布局，显示用户保存的所有动画
每个卡片显示：
  • 封面图（用户上传的题目图片）
  • 动画名称
操作：
  • 点击卡片 → 加载到画布
  • 悬浮显示更多操作：
    - 分享按钮 → 生成分享链接
    - 上传按钮 → 发布到广场（如果未发布）
    - 下架按钮 → 从广场移除（如果已发布）
    - 删除按钮 → 删除动画
```

#### 「动画广场」面板

```
位置：页面底部
展示：横向滚动的卡片列表
每个卡片显示：
  • 封面图
  • 动画名称
  • ❤️ 点赞数（可直接点击点赞）
卡片状态：
  • 默认状态：正常大小
  • 选中状态：微微放大（scale: 1.05）+ 高亮边框（蓝色/主题色）
操作：
  • 点击卡片 → 直接加载到画布 + 进入选中状态
  • 点击 ❤️ → 点赞（需登录）
```

#### 画布下方信息区

```
显示条件：加载了广场动画时显示
内容：
  ┌─────────────────────────────────────────────────────────┐
  │  📝 动画名称                           ❤️ 点赞数  👤 作者 │
  │  动画描述文字...                                         │
  └─────────────────────────────────────────────────────────┘
  
  - 动画名称：标题样式
  - 动画描述：正文样式，可多行
  - ❤️ 点赞数：可点击进行点赞
  - 👤 作者：如果作者选择公开用户名则显示，否则不显示
```

### 2.5 登录状态对功能的影响

| 功能 | 未登录 | 已登录 |
|------|--------|--------|
| 使用物理模拟画布（上传、分析、模拟） | ✅ 可以 | ✅ 可以 |
| 点击「下载」保存动画 | ❌ 提示登录 | ✅ 可以 |
| 查看「我的动画」面板 | 显示空/提示登录 | ✅ 显示列表 |
| 查看「动画广场」 | ✅ 可以 | ✅ 可以 |
| 点击广场动画加载到画布 | ✅ 可以 | ✅ 可以 |
| 从广场「下载」（Fork） | ❌ 提示登录 | ✅ 可以 |
| 点赞 | ❌ 提示登录 | ✅ 可以 |
| 分享链接 | - | ✅ 可以 |

---

## 3. 核心概念：什么是一个"动画"

一个"动画"是已经处理好的模拟数据，包含直接运行所需的所有信息：

```json
{
  "version": "1.0",
  "canvas": { "width": 800, "height": 600 },
  "thumbnail_url": "原始上传图片的URL或base64",
  "background_url": "data:image/png;base64,...",
  "objects": [
    {
      "name": "滑块A",
      "role": "dynamic",
      "sprite_data_url": "data:image/png;base64,...",
      "contour": [{"x": 100, "y": 200}, {"x": 150, "y": 200}, ...],
      "parameters": {
        "mass": 1,
        "friction": 0.5,
        "restitution": 0.8,
        "initial_velocity": {"x": 5, "y": 0}
      },
      "is_concave": false
    }
  ],
  "constraints": [
    {
      "bodyName": "摆球",
      "pivotName": "天花板",
      "pivotPoint": {"x": 400, "y": 50},
      "constraintType": "pendulum",
      "length": 200
    }
  ]
}
```

---

## 4. 数据库设计

### 4.1 animations 表（动画主表）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | String | Primary Key | 动画唯一ID（UUID） |
| `user_id` | Integer | Foreign Key | 创建者ID（关联 users 表） |
| `title` | String | Not Null | 动画名称 |
| `description` | Text | Optional | 描述/说明 |
| `thumbnail_url` | String | Not Null | 封面图地址（用户上传的题目图片） |
| `scene_data` | JSON/Text | Not Null | **核心**：完整的模拟数据 |
| `share_code` | String | Unique | 分享码（用于生成链接） |
| `is_public` | Boolean | Default False | 是否公开（动画广场可见） |
| `show_author` | Boolean | Default False | 是否显示作者用户名 |
| `fork_from` | String | Optional | 如果是Fork的，记录原动画ID |
| `like_count` | Integer | Default 0 | 点赞数（冗余字段，方便排序） |

### 4.2 animation_likes 表（点赞记录）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| `id` | Integer | Primary Key | 记录ID |
| `user_id` | Integer | Foreign Key | 点赞用户ID |
| `animation_id` | String | Foreign Key | 动画ID |

> **唯一约束**：(user_id, animation_id) 防止重复点赞

---

## 5. 系统架构

```
                    ┌──────────────────────┐
                    │      用户系统         │
                    │   (登录/注册/鉴权)    │
                    └──────────┬───────────┘
                               │
        ┌──────────────────────┼──────────────────────┐
        │                      │                      │
        ▼                      ▼                      ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│   我的动画     │    │   动画广场     │    │   物理模拟     │
│  (私有空间)    │    │  (公共空间)    │    │  (创作工具)    │
├───────────────┤    ├───────────────┤    ├───────────────┤
│ • 查看列表     │    │ • 浏览动画     │    │ • 上传图片     │
│ • 加载运行     │    │ • 点击加载     │    │ • 大模型分析   │
│ • 编辑修改     │    │ • 卡片点赞     │    │ • 框选元素     │
│ • 删除动画     │    │ • 下载(Fork)   │    │ • OpenCV处理   │
│ • 上传到广场   │    │               │    │ • 运行模拟     │
│ • 分享链接     │    │               │    │ • 下载保存     │
└───────────────┘    └───────────────┘    └───────────────┘
```

---

## 6. 数据隔离策略

### 「我的动画」与「动画广场」隔离原则

| 操作 | 我的动画 | 动画广场 |
|------|---------|---------|
| 可见性 | 仅自己可见 | 所有人可见 |
| 编辑权限 | 可编辑/删除 | 只能下载（Fork）后编辑 |
| 上传 | 可上传到广场 | - |
| 下载 | - | 下载到我的动画（Fork） |

### Fork 机制（通过「下载」按钮触发）

当用户点击广场动画的「下载」按钮时：
1. 创建新记录：`user_id = 当前用户`
2. 设置为私有：`is_public = false`
3. 记录来源：`fork_from = 原动画ID`
4. 复制数据：`scene_data = 原动画数据的深拷贝`
5. 复制封面：`thumbnail_url = 原动画封面`

Fork 后的动画与原动画**完全独立**，修改不互相影响。

---

## 7. API 接口设计

### 7.1 我的动画

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/animations/mine` | 获取我的动画列表 |
| POST | `/api/animations` | 创建新动画（下载/保存） |
| GET | `/api/animations/{id}` | 获取动画详情（含 scene_data） |
| PUT | `/api/animations/{id}` | 更新动画 |
| DELETE | `/api/animations/{id}` | 删除动画 |
| POST | `/api/animations/{id}/publish` | 发布到广场 |
| POST | `/api/animations/{id}/unpublish` | 从广场下架 |

### 7.2 动画广场

| 方法 | 路径 | 功能 |
|------|------|------|
| GET | `/api/plaza/animations` | 获取广场动画列表 |
| GET | `/api/plaza/animations/{id}` | 获取单个动画详情 |
| POST | `/api/plaza/animations/{id}/fork` | Fork到我的动画（下载按钮调用） |
| POST | `/api/plaza/animations/{id}/like` | 点赞 |
| DELETE | `/api/plaza/animations/{id}/like` | 取消点赞 |

### 7.3 分享链接

| 方法 | 路径 | 功能 |
|------|------|------|
| POST | `/api/animations/{id}/share-link` | 生成分享链接 |
| GET | `/api/play/{share_code}` | 通过分享码获取动画数据 |

---

## 8. 前端路由规划

```
/physics                    → 物理模拟主页面（当前已有）
/physics/play/:shareCode    → 分享链接播放页（精简UI，专注播放）
```

---

## 9. 技术实现要点

### 9.1 保存动画时捕获数据

```javascript
// 从当前模拟状态中提取数据
function captureSceneData() {
  const cached = simulationCache.current?.data;
  
  return {
    version: "1.0",
    canvas: { 
      width: imageNaturalSize.w, 
      height: imageNaturalSize.h 
    },
    thumbnail_url: originalImageUrl,  // 用户上传的原始图片
    background_url: cached?.background_clean_data_url,
    objects: cached?.objects?.map(obj => ({
      name: obj.name,
      role: obj.role,
      sprite_data_url: obj.sprite_data_url,
      contour: obj.contour,
      parameters: obj.parameters,
      is_concave: obj.is_concave
    })),
    constraints: constraintRelations
  };
}
```

### 9.2 加载动画时恢复场景

```javascript
// 根据 scene_data 恢复模拟场景
function loadAnimation(sceneData, animationInfo) {
  // 1. 设置背景
  setImagePreview(sceneData.background_url);
  setImageNaturalSize(sceneData.canvas);
  
  // 2. 设置当前加载的动画信息（用于显示信息区）
  setCurrentAnimation(animationInfo);  // { title, description, like_count, author_name }
  
  // 3. 显示下载按钮
  setShowDownloadBtn(true);
  
  // 4. 缓存数据以便直接运行
  simulationCache.current = {
    key: 'loaded-animation',
    data: {
      objects: sceneData.objects,
      background_clean_data_url: sceneData.background_url
    }
  };
}

// 运行动画
function runLoadedAnimation() {
  runSimulation({
    container: simRef.current,
    objects: sceneData.objects,
    constraints: sceneData.constraints,
    imageRect: imgRef.current?.getBoundingClientRect?.(),
    naturalSize: sceneData.canvas,
  });
}
```

### 9.3 广场卡片选中状态

```css
/* 广场动画卡片 */
.plaza-card {
  transition: transform 0.2s, box-shadow 0.2s;
}

/* 选中状态 */
.plaza-card.selected {
  transform: scale(1.05);
  box-shadow: 0 0 0 3px #3b82f6, 0 4px 12px rgba(59, 130, 246, 0.3);
}
```

### 9.4 下载按钮逻辑

```javascript
function handleDownload() {
  if (!isLoggedIn) {
    showLoginPrompt();
    return;
  }
  
  if (currentAnimation?.isFromPlaza) {
    // 从广场加载的动画 → Fork
    forkAnimation(currentAnimation.id);
  } else {
    // 自己创建的模拟 → 保存
    showSaveModal();
  }
}
```

---

## 10. 开发阶段规划

### 第一阶段：基础存储（依赖登录功能）
1. 创建 animations 数据库表
2. 实现 captureSceneData 函数
3. 实现「下载」按钮（首次模拟后显示）
4. 实现保存弹窗（自动使用上传图片作为封面）
5. 实现"我的动画"面板展示
6. 实现 loadAnimation 函数

### 第二阶段：动画广场
1. 动画广场面板展示
2. 卡片点击 → 直接加载到画布
3. 卡片选中状态（放大+高亮）
4. 画布下方信息区
5. 点赞功能（卡片上 + 信息区）
6. 下载按钮复用（Fork功能）

### 第三阶段：分享功能
1. 链接分享功能
2. 分享链接播放页

---

## 11. 依赖关系

```
登录功能 ──▶ 我的动画 ──┬──▶ 动画广场
                       │
                       └──▶ 链接分享
```

---

## 12. 文件结构规划

```
backend/app/
├── models/
│   ├── user.py              # 用户模型
│   └── animation.py         # 动画模型
├── routers/
│   ├── auth_router.py       # 认证路由
│   ├── animation_router.py  # 我的动画路由
│   └── plaza_router.py      # 动画广场路由
└── ...

frontend/src/
├── components/
│   ├── Auth/                    # 登录组件
│   ├── Animation/               # 动画相关组件
│   │   ├── MyAnimations.jsx     # 我的动画面板
│   │   ├── AnimationCard.jsx    # 动画卡片
│   │   ├── Plaza.jsx            # 动画广场面板
│   │   ├── PlazaCard.jsx        # 广场卡片（含选中状态）
│   │   ├── SaveModal.jsx        # 保存弹窗
│   │   ├── AnimationInfo.jsx    # 画布下方信息区
│   │   └── DownloadButton.jsx   # 下载按钮
│   └── ...
├── pages/
│   └── PlayPage.jsx             # 分享播放页
├── store/
│   ├── authStore.js             # 用户状态
│   └── animationStore.js        # 动画状态
└── ...
```

---

## 13. 暂缓功能（后续迭代）

以下功能本次不实现，留待后续迭代：
- [ ] 官方模板
- [ ] 标签系统
- [ ] 播放计数
- [ ] 动画广场搜索/筛选
- [ ] 视频导出
- [ ] GIF导出

---

## 14. 总结

| 评估项 | 结论 |
|--------|------|
| **整体可行性** | ✅ 完全可行 |
| **技术匹配度** | 与现有 simulationCache 机制完美契合 |
| **用户体验** | 简化流程，复用按钮，减少弹窗 |
| **依赖前置** | 需先完成登录功能 |
| **建议优先级** | 登录 → 下载按钮 → 我的动画 → 广场 → 链接分享 |
